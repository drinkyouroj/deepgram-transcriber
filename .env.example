# Deepgram API Configuration
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Optional: Set default language (can be overridden via CLI)
DEFAULT_LANGUAGE=en

# Optional: Set default model (can be overridden via CLI)
DEFAULT_MODEL=nova-2